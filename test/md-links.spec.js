const mdLinks = require('../');


describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });
  it('debería devolver una promesa', () => {
    expect(mdLinks).toBe(Promise);
  });
  it('si no hay ruta, se rechaza la promesa'),() => {
    expect(mdLinks)
  }
});
